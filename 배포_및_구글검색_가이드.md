# 셔틀빌리지 — 배포 · 구글 검색 노출 · 홈 화면 추가 가이드

이 문서 하나만 순서대로 따라 하면 됩니다. **개발 지식 없어도 됩니다.**
사장님이 직접 해야 하는 일에는 🙋 표시를 해 두었습니다.

---

## 0. 지금 내 PC에서 실행해 보기

바탕화면 `shuttle-village` 폴더에서:

```bash
npm run dev
```

브라우저가 열리면 `http://localhost:5180` 으로 접속됩니다.
휴대폰에서 미리 보고 싶으면, PC와 휴대폰을 **같은 와이파이**에 연결한 뒤:

```bash
npm run dev -- --host
```

터미널에 뜨는 `Network: http://192.168.x.x:5180` 주소를 휴대폰 브라우저에 입력하면 됩니다.

---

## 1. 인터넷에 올리기 (Vercel — 무료, 5분)

기존 배드민턴 앱과 같은 방식입니다.

### 1-1. 깃허브에 올리기 🙋

```bash
cd C:\Users\USER\OneDrive\Desktop\shuttle-village
git init
git add .
git commit -m "셔틀빌리지 첫 배포"
```

그 다음 깃허브에서 새 저장소(예: `shuttle-village`)를 만들고:

```bash
git remote add origin https://github.com/jungjin20020506/shuttle-village.git
git branch -M main
git push -u origin main
```

### 1-2. Vercel 연결 🙋

1. https://vercel.com 접속 → **Continue with GitHub** 로 로그인
2. **Add New… → Project** 클릭
3. 방금 만든 `shuttle-village` 저장소 옆 **Import** 클릭
4. 설정은 건드릴 것 없습니다 (Vite 자동 인식). **Deploy** 클릭
5. 1~2분 뒤 `https://shuttle-village.vercel.app` 같은 주소가 나옵니다

> 앞으로는 `git push` 만 하면 자동으로 새 버전이 배포됩니다.

### 1-3. 주소를 정한 뒤 꼭 해야 할 일 🙋

배포 주소가 확정되면 아래 **3개 파일**에서 `https://shuttle-village.vercel.app` 를
**실제 주소로 전부 바꿔주세요.** (검색 노출에 직접 영향을 줍니다)

| 파일 | 바꿀 곳 |
|---|---|
| `index.html` | `canonical`, `og:url`, `og:image`, `twitter:image`, 구조화 데이터의 `url`·`image` |
| `public/robots.txt` | `Sitemap:` 줄 |
| `public/sitemap.xml` | `<loc>` 줄 |

바꾼 뒤 다시 `git add . → git commit → git push` 하면 반영됩니다.

### 1-4. (선택) 내 도메인 연결 🙋

`shuttlevillage.kr` 같은 주소를 쓰고 싶다면 가비아·호스팅케이알 등에서 도메인을 사고,
Vercel 프로젝트 → **Settings → Domains** 에서 추가하면 됩니다.
**검색 노출은 짧고 기억하기 쉬운 도메인이 훨씬 유리합니다.**

---

## 2. 구글에 "셔틀 빌리지" 검색하면 나오게 하기

> ⚠️ 사이트를 만들어 두기만 하면 구글은 **자동으로 찾아주지 않습니다.**
> 아래 2-1을 꼭 해야 하고, 보통 **등록 후 3일 ~ 2주** 뒤부터 검색에 나옵니다.

### 2-1. 구글 서치 콘솔 등록 (가장 중요) 🙋

1. https://search.google.com/search-console 접속 → 구글 계정으로 로그인
2. 왼쪽 위 **속성 추가** → **URL 접두어** 선택 → 배포 주소 입력 (예: `https://shuttle-village.vercel.app`)
3. **소유권 확인** 화면이 나옵니다 → **HTML 태그** 방식을 선택하면
   `<meta name="google-site-verification" content="어쩌구저쩌구" />` 같은 줄을 줍니다
4. 그 줄을 복사해서 `index.html` 의 `<head>` 안(다른 meta 태그들 옆)에 붙여넣고
   `git push` 로 배포합니다
5. 배포가 끝나면 서치 콘솔에서 **확인** 버튼을 누릅니다 → 소유권 확인 완료

### 2-2. 사이트맵 제출 🙋

서치 콘솔 왼쪽 메뉴 → **Sitemaps** → `sitemap.xml` 입력 → **제출**

### 2-3. 색인 요청 (빠르게 올리기) 🙋

서치 콘솔 맨 위 검색창에 배포 주소를 넣고 → **색인 생성 요청** 클릭.
이걸 해두면 며칠 안에 검색 결과에 뜨기 시작합니다.

### 2-4. 네이버·다음에도 등록 🙋 (한국에서는 이게 더 중요할 수 있습니다)

- 네이버: https://searchadvisor.naver.com → 웹마스터도구 → 사이트 등록 → 소유확인 →
  **사이트맵 제출**(`sitemap.xml`) + **웹페이지 수집 요청**
- 다음(카카오): https://register.search.daum.net/index.daum 에서 사이트 등록

### 2-5. 검색 순위를 올리는 현실적인 방법

"셔틀 빌리지"는 흔한 단어 조합이라, 처음에는 다른 페이지에 밀릴 수 있습니다.
아래를 같이 하면 훨씬 빨리 1페이지로 올라옵니다.

1. **다른 곳에서 링크 걸기** — 클럽 카페·블로그·인스타그램 프로필·소모임 공지에
   배포 주소를 올려두세요. 구글은 "다른 사이트가 링크한 사이트"를 훨씬 신뢰합니다.
2. **이름을 고유하게** — 가능하면 "셔틀빌리지" 를 붙여 쓰고, 소개 문구에
   `배드민턴`, `클럽`, `매칭` 같은 단어를 자연스럽게 섞어주세요. (이미 넣어뒀습니다)
3. **도메인 이름에 넣기** — `shuttlevillage.kr` 처럼 이름이 들어간 도메인이 가장 강력합니다.
4. **꾸준한 접속** — 실제로 사람들이 들어오고 머무는 사이트가 위로 올라갑니다.

---

## 3. 휴대폰에서 "앱처럼" 설치하기 (홈 화면에 추가)

설치 파일(APK)을 배포할 필요가 **전혀 없습니다.** 주소만 열면 앱이 됩니다.

### 3-1. 회원들에게 안내할 문구 (그대로 복사해서 단톡방에 올리세요)

```
🏸 셔틀빌리지 오픈했습니다!

📱 설치 방법 (30초)
1. 아래 주소를 눌러서 접속
   https://shuttle-village.vercel.app

2. 홈 화면에 추가하기
   · 안드로이드(크롬): 화면에 뜨는 "홈 화면에 추가" 버튼을 누르거나,
     오른쪽 위 ⋮ → "앱 설치" / "홈 화면에 추가"
   · 아이폰(사파리): 아래 공유 버튼 ⬆️ → "홈 화면에 추가"

3. 홈 화면에 🏸 아이콘이 생기면 끝!
   앱처럼 아이콘만 눌러서 바로 들어오면 됩니다.

⚠️ 카카오톡 안에서 열면 설치가 안 돼요.
   링크를 누른 뒤 오른쪽 아래 "다른 브라우저로 열기"를 눌러주세요.
```

### 3-2. 앱 안에서도 자동으로 안내됩니다

접속 후 15초쯤 지나면 마을 안내원 코코가 **"홈 화면에 추가"** 를 직접 권합니다.
안드로이드는 버튼 한 번으로 설치되고, 아이폰은 방법을 그림처럼 설명해 줍니다.
"나중에"를 누르면 그날은 다시 뜨지 않습니다.

### 3-3. 홈 화면 아이콘

`public/` 폴더에 이미 만들어 두었습니다. (잔디밭 위 셔틀콕 디자인)

| 파일 | 용도 |
|---|---|
| `icon-192.png` / `icon-512.png` | 안드로이드 홈 화면·앱 서랍 |
| `icon-maskable-512.png` | 안드로이드 동그란 아이콘용(잘려도 안전) |
| `apple-touch-icon.png` | 아이폰 홈 화면 |
| `og-image.png` | 카카오톡·페북 공유 시 뜨는 미리보기 |

아이콘 디자인을 바꾸고 싶으면 `tools/make-icons.cjs` 를 수정하고:

```bash
node tools/make-icons.cjs
```

---

## 4. 자주 묻는 질문

**Q. 카톡으로 링크를 보냈는데 설치 버튼이 안 떠요**
A. 카카오톡 내장 브라우저는 PWA 설치를 지원하지 않습니다. 링크를 연 뒤
오른쪽 아래 **"다른 브라우저로 열기"**(크롬/사파리)를 누르면 정상 동작합니다.

**Q. 데이터는 어디에 저장되나요?**
A. 지금은 **휴대폰 안(localStorage)** 에 저장됩니다. 폰을 바꾸면 기록이 넘어가지 않고,
회원끼리 같은 마을을 공유하지도 않습니다.
→ 여러 사람이 같은 마을을 함께 쓰려면 Firebase 연동이 필요합니다 (콕스타와 같은 방식).

**Q. 게임이 느려요 / 폰이 뜨거워져요**
A. 게임 안 **⚙️ 설정 → 그래픽 품질 → 가볍게** 로 바꿔 주세요.

**Q. 처음부터 다시 하고 싶어요**
A. **⚙️ 설정 → 마을 초기화** 를 누르면 됩니다.
