# 셔틀빌리지 ↔ 콕스타(COCKSTAR) 연동 가이드

셔틀빌리지는 **콕스타와 같은 Firebase 프로젝트(`noerror-14ce3`)** 를 씁니다.
따로 회원을 만들 필요가 없고, 콕스타 계정과 경기방을 그대로 씁니다.

---

## 1. 무엇이 공유되나

| 데이터 | 경로 | 공유 여부 |
|---|---|---|
| 회원 계정(로그인) | Firebase Auth | ✅ 완전히 동일 |
| 선수 프로필 (이름·급수·성별·출생연도·지역) | `users/{uid}` | ✅ 양방향 |
| 경기방 목록 | `rooms` | ✅ 양방향 |
| 방 참가자 | `rooms/{roomId}/players/{uid}` | ✅ 양방향 |
| 코트 배치·경기 진행 | `rooms/{roomId}.inProgressCourts` | ✅ 양방향 |
| 오늘 경기 수·경기 기록 | `players.todayGames`, `matchHistory` | ✅ 양방향 |
| 캐릭터 3D 외모 | `users/{uid}.svLook`, `players.svLook` | 셔틀빌리지 전용 (콕스타는 무시) |

**콕스타에서 선수를 코트에 넣으면, 셔틀빌리지에서 그 선수 캐릭터가 코트로 걸어갑니다.**
반대로 셔틀빌리지에서 경기를 시작·종료하면 콕스타 화면에도 그대로 반영됩니다.

---

## 2. 로그인 방식 (콕스타와 동일)

- **💬 카카오 로그인** — 콕스타와 같은 OIDC 공급자(`oidc.kakao`)
- **📱 휴대폰 번호** — SMS 인증번호 (콕스타와 동일)
- **시스템 관리자 전용 로그인** — 아이디/비밀번호 (하단 작은 글씨)
  - 아이디 변환 규칙도 동일: `domain` → `domain@special.user`, 그 외 → `아이디@cockstar.app`

처음 로그인하면 콕스타와 **똑같은 항목**(이름·급수·성별·출생연도·지역)을 받아
`users/{uid}` 문서를 만듭니다. 필드 구성이 같기 때문에 콕스타에서 다시 물어보지 않습니다.

> 로그인하지 않아도 **혼자 노는 마을**(자동매칭·상점·미니게임·꾸미기)은 그대로 즐길 수 있습니다.

---

## 3. 사용 순서

1. 하단 **☰ 더보기 → ⚙️ 설정 → 🔑 콕스타 계정으로 로그인**
2. 카카오 또는 휴대폰으로 로그인
3. **☰ 더보기 → 🏸 콕스타 경기방**
4. 콕스타에서 만든 방 목록이 그대로 보임 → **입장** (비밀번호 방이면 비밀번호 입력)
5. 3D 마을이 그 경기방으로 바뀌고, 참가자들이 각자 캐릭터로 나타남
6. **🏸 경기 메뉴**에서
   - 참가자: 내 휴식/복귀
   - 방 관리자: 코트 수 변경, 앞 4명 투입, 경기 종료

---

## 4. 🙋 배포 전 반드시 해야 할 일 (중요)

### 4-1. Firebase 승인된 도메인 추가

로그인이 **`localhost`에서는 되지만 배포 주소에서는 안 되는** 문제를 막으려면:

1. https://console.firebase.google.com → 프로젝트 **noerror-14ce3** 선택
2. 왼쪽 **Authentication → Settings(설정) → 승인된 도메인(Authorized domains)**
3. **도메인 추가** 를 눌러 셔틀빌리지 배포 주소를 넣기
   - 예: `shuttle-village.vercel.app`
   - 나중에 개인 도메인을 붙이면 그것도 추가

> 이걸 안 하면 카카오 로그인 팝업이 `auth/unauthorized-domain` 오류로 막힙니다.

### 4-2. 카카오 개발자 콘솔에 Redirect URI 추가

카카오 로그인을 쓰려면 카카오 쪽에도 새 주소를 등록해야 합니다.

1. https://developers.kakao.com → 내 애플리케이션 → 콕스타 앱 선택
2. **카카오 로그인 → Redirect URI** 에 아래 주소 추가
   - `https://noerror-14ce3.firebaseapp.com/__/auth/handler`
   - (이미 콕스타용으로 등록돼 있다면 그대로 두면 됩니다 — Firebase가 중간에서 처리하므로
     셔틀빌리지 주소를 따로 넣을 필요는 없습니다)

### 4-3. Firestore 보안 규칙 확인

지금 규칙은 **로그인해야 방 목록을 읽을 수 있게** 되어 있습니다(정상).
셔틀빌리지가 추가로 쓰는 필드는 `svLook` 하나뿐이라, 규칙을 바꿀 필요는 없습니다.
다만 규칙에서 **필드를 화이트리스트로 제한**하고 있다면 `svLook` 을 허용 목록에 넣어야 합니다.

---

## 5. 알아둘 점 · 한계

- **방 비밀번호는 안전장치가 아닙니다.** 콕스타와 동일하게 클라이언트에서 문자열만 비교합니다.
  (방 문서를 읽을 수 있으면 비밀번호도 보입니다 — 콕스타의 기존 구조를 그대로 따랐습니다)
- **정원·급수 제한은 표시만** 되고 실제 입장을 막지 않습니다. (콕스타와 동일)
- **`playerCount` 필드는 콕스타에서 항상 0** 이라 셔틀빌리지도 이 값을 쓰지 않고,
  실제 참가자 수는 `players` 하위 문서를 세어 보여줍니다.
- 셔틀빌리지에서 **경기방을 새로 만들면** 콕스타 목록에도 뜨지만,
  주소 검색(카카오 지도)이 없어 **콕맵 지도에는 표시되지 않습니다.**
  지도에 띄우려면 콕스타에서 그 방의 주소를 한 번 저장해 주세요.
- 경기방 모드에서는 **점수 대신 경과 시간**이 코트 위에 표시됩니다.
  콕스타에는 승패 개념이 없기 때문입니다. (혼자 놀기 마을에서는 점수 경기 그대로 동작)
- 경기방에 있는 동안에는 셔틀빌리지의 자동매칭 시뮬레이션이 멈춥니다.
  경기 진행 권한은 콕스타와 마찬가지로 **방 관리자**에게 있습니다.

---

## 6. 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| 방 목록이 "Missing or insufficient permissions" | 로그인이 안 된 상태입니다. 먼저 로그인하세요. |
| 카카오 로그인 팝업이 바로 닫힘 | 4-1의 승인된 도메인에 배포 주소를 추가하세요. |
| 인증번호 SMS가 안 옴 | Firebase 콘솔 → Authentication → Sign-in method 에서 **전화** 공급자가 켜져 있는지, 일일 발송 한도를 넘지 않았는지 확인하세요. |
| 콕스타에서 프로필 입력창이 다시 뜸 | `users` 문서 필드가 빠진 경우입니다. 셔틀빌리지는 콕스타와 같은 9개 필드를 저장하므로 정상적으로는 발생하지 않습니다. |
| 내 캐릭터 외모가 다른 기기에서 다름 | 로그인 상태에서 옷장을 한 번 열었다 닫으면 `svLook` 이 계정에 저장됩니다. |
