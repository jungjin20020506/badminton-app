# 셔틀몬스터 — 아이디어 백로그

> 목표: **경기를 기다리는 지루한 시간에 이 앱을 계속 켜 두게 만드는 것.**
> 우리나라 최고의 배드민턴 모임 연동 게임이 되는 게 방향이다.
>
> 절대 규칙 — ① 승패 기록 기능은 만들지 않는다(성장·보상은 오직 **참여**로만)
> ② 저장 키 `shuttle-village-save-v1` 를 바꾸지 않는다
> ③ 콕스타와 공유하는 Firestore 문서 구조를 바꾸지 않는다
> ④ `git push --force` 금지

---

## 완료

- **2026-07-29 · 미니게임 「셔틀 랠리」 + 🎮 놀거리 메뉴**
  마을 주민을 골라 셔틀콕을 주고받는 타이밍 미니게임. 승패가 없다 —
  랠리는 이기는 게 아니라 끊기지 않게 **같이** 이어 가는 것이라, 이 게임의 규칙과 맞는다.
  - `src/game/rally.js` — 판정 규칙만 따로 뺀 순수 모듈. 판정은 오직 시계로만 계산하므로
    화면이 버벅여도 타이밍이 밀리지 않는다(rAF 는 그림만 담당).
  - 보상은 리프팅과 **같은 하루 300🪙 지갑**을 쓴다. 랠리 1회 6🪙 + PERFECT 3🪙.
  - 오래 이어 가면 그 주민과 **친밀도**가 오른다. 단 하루 한 사람당 한 번
    (`today.rallyFriends`) — 한 사람만 붙잡고 돌리면 의미가 없으니까.
  - 트로피 2종 추가 (`t_rally15` 랠리 파트너 / `t_rally40` 끝나지 않는 랠리).
  - 미니게임·뽑기가 마을 구석 NPC 대화로만 열려서 정작 벤치에서 못 찾던 문제 →
    START 메뉴에 **🎮 놀거리** 추가. 아무도 안 열던 죽은 `더보기` 패널을 이걸로 교체.
  - 곁다리 버그: 대진표(`MatchBoard`)가 1초 타이머를 `useMemo` 로 걸어 두어
    정리 함수가 안 불리고, 열 때마다 인터벌이 하나씩 쌓이던 문제 → `useEffect` 로 수정.

### 그 이전 (커밋 로그 기준)

- 캐릭터 베이스 재설계(3.3등신) + 관리자 전권 모드
- 경기방 콕스타 구조 이식 · 관리자 로그인 수정 · 초레어템 5종
- 카메라 후퇴 · 미니맵 · 64x96 정밀 스프라이트
- HD-2D 전환 — 도트 스프라이트를 실제 3D 공간에 세움, 틸트시프트·블룸
- 경기방 도킹 창(`RoomDock`) · 내 순번/예상 대기시간(`queue.js`) · 차례 알림

---

## 백로그

### 대기 시간 (최우선)
- [ ] **랠리 2단계** — 지금은 상대가 무조건 받아 넘겨 준다. 상대의 급수/친밀도에 따라
      리턴 속도·각도가 달라지게. 친할수록 「받기 좋은 공」을 준다는 설정이면 친밀도가 더 의미 있어진다.
- [ ] **랠리 랭킹** — 마을 랭킹 패널(`rank`)에 「최장 랠리」 줄 추가. 지금 `bestRally` 만 저장 중.
- [ ] **함께 랠리** — 경기방에 있는 다른 사람과 같은 시각에 랠리하면 둘 다 보너스
      (Firestore 문서 구조를 못 바꾸니, 방 문서를 읽기만 하고 시간대만 맞춰 보는 식으로)
- [ ] **셔틀콕 줍기** — 마을을 돌아다니다 보면 바닥에 셔틀콕/깃털이 떨어져 있고 주우면 코인.
      시간이 지나면 다시 생긴다. 이동 자체에 이유를 준다.
- [ ] **벽치기** — 체육관 벽에 대고 혼자 랠리. 상대를 못 고를 때(주민 0명) 대안.

### 경기방 사용성
- [ ] 대진표 「경기 예정」에 **각 매치의 예상 시작 시각**을 적어 준다 (평균 경기 시간 기반).
- [ ] 대기 명단 카드에 **내 앞 순번 번호**를 직접 찍어 준다 (지금은 순번 카드에만 한 줄).
- [ ] 코트별 진행 시간이 평균을 넘기면 카드 색이 변해서 「곧 끝난다」가 보이게.

### 그래픽 · UI
- [ ] 밤 시간대 가로등/창문 불빛 (`daylightTint` 만으로는 밤이 그냥 어둡기만 하다).
- [ ] 모바일 세로에서 START 메뉴 항목이 15개라 스크롤이 길다 — 2열 그리드 검토.
- [ ] 코트 위 캐릭터가 실제로 셔틀콕을 주고받는 모션 (지금은 서 있기만 한다).

### 개발 환경 메모 (중요)
- 브라우저 패널이 화면에 보이지 않으면 **rAF 도 ResizeObserver 도 아예 안 돈다.**
  그래서 `@react-three/fiber` 의 `<Canvas>` 가 크기를 0으로 재고 **아무것도 마운트하지 않는다**
  (`__svShoot` 도 정의되지 않는다). 3D 화면은 이 상태에서 눈으로 확인할 방법이 없다.
- 반면 **React DOM 렌더 · setTimeout · 이벤트 디스패치는 정상 동작**한다.
  → UI 패널/미니게임은 `document.querySelector(...).click()` 과
    `dispatchEvent(new PointerEvent('pointerdown'))` 로 끝까지 몰아 볼 수 있다.
  → 그래서 랠리 판정을 rAF 가 아니라 `performance.now()` 기준으로 짰다. 앞으로도 이 원칙을 지키자.
- zustand 셀렉터가 매번 새 배열/객체를 돌려주면
  `The result of getSnapshot should be cached` 무한 렌더로 컴포넌트가 통째로 안 뜬다.
  (`(s) => s.x || []` 금지 — 원본을 그대로 꺼내고 기본값은 쓰는 쪽에서.)
