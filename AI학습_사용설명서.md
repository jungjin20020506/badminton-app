# 🧠 KNK AI 학습(RAG) 사용설명서

> 코딩 몰라도 됩니다. 이 문서만 따라 하면 우리 회사 AI가 점점 똑똑해집니다.

---

## 1. 한 줄 요약

AI에게 **우리 회사 지식(검사기·품질)을 가르치면**, 나중에 질문할 때 AI가 그 지식을
찾아서 **근거로 삼아 답합니다.** 이 방식을 **RAG(검색 증강 생성)** 라고 부릅니다.

- AI의 뇌를 바꾸는 게 아니라 → **AI에게 줄 "참고서"를 만드는 것**입니다.
- 그래서 나중에 더 똑똑한 AI로 바꿔도, **가르쳐 둔 지식은 그대로** 쓸 수 있습니다.

---

## 2. AI 담당 직원이 적어준 용어 풀이 (초보자용)

| 담당자가 적은 말 | 쉬운 설명 | 이 프로그램에서 |
|---|---|---|
| **RAG** | 질문에 맞는 자료를 **찾아서(검색)** AI에게 주고 답하게 하는 방식 | ✅ 그대로 구현됨 |
| **Embedding model** (임베딩) | 문장을 **"의미 좌표(숫자)"** 로 바꾸는 도구. 그래야 뜻이 비슷한 걸 찾을 수 있음 | ✅ OpenAI `text-embedding-3-small` |
| **Vector DB** (벡터DB) | 그 "의미 좌표"를 저장해 두는 창고 | ✅ 프로그램 DB 안 `knowledge` 표 |
| **langchain** | 위 과정을 대신 엮어주는 **선택적 도구(설치 필요)** | ⚠ 아래 설명 참고 |

### langchain은 왜 안 썼나요? (담당자에게 전달용)
langchain은 편의 도구일 뿐이고, 하는 일은 위 표의 RAG·임베딩·벡터DB를 **엮는 것**입니다.
그런데 이 프로그램은 **"설치 없이 여러 PC에서 바로 실행"** 되는 게 핵심 장점입니다
(파이썬 표준 기능만 사용). langchain·전용 벡터DB를 넣으면 **PC마다 추가 설치**가 필요해져
그 장점이 깨집니다. 그래서 **결과는 완전히 동일한** RAG를 표준 기능만으로 직접 구현했습니다.
→ **키워드가 안 겹쳐도 "의미"로 찾아주는** 진짜 RAG가 이미 동작합니다(테스트 완료).
나중에 지식이 수만 건으로 커지면 그때 전용 벡터DB로 옮기면 됩니다.

---

## 3. 어디 있나요?

프로그램 상단 메뉴 **`🧠 AI 학습`** 버튼입니다. (🤖 AI 도우미 옆)

화면은 3칸입니다:
1. **✍️ 직접 가르치기** — 아는 내용을 적어 넣기
2. **🎤 AI 인터뷰** — AI가 질문하면 전문가가 답 (가장 빠르게 쌓는 방법)
3. **📚 쌓인 지식 보기 / 삭제**

---

## 4. 가르치는 3가지 방법

### 방법 A. 직접 적어 넣기 (가장 간단)
1. `🧠 AI 학습` 화면 → **✍️ 직접 가르치기**
2. (선택) 주제 칸에 `방수검사`, `판정기준` 처럼 분류를 적음
3. 큰 칸에 아는 내용을 **그냥 문장으로** 적음 (길어도 됨. 자동으로 나눠 저장)
4. **[＋ 지식 추가]** 클릭 → 끝

> 💡 팁: **한 번에 한 가지 주제**만 적으면 나중에 검색이 더 정확합니다.

### 방법 B. AI 인터뷰 (⭐ 추천 — 짧은 시간에 고품질로 쌓기)
품질 전문가와 함께 앉아서:
1. **[🔄 다음 질문 받기]** 클릭 → AI가 질문을 하나 만들어 줌
2. 전문가가 **말하듯 답을 적음**
3. **[💾 답변 저장하고 다음]** 클릭 → AI가 답을 **핵심만 정리해서** 저장하고, 곧바로 다음 질문

AI는 **이미 아는 건 안 묻고, 비어 있는 부분**을 골라 질문합니다.
처음엔 넓게(검사기 종류·검사 방식) 묻다가, 지식이 쌓일수록 **점점 더 깊고 구체적인 질문**으로
좁혀갑니다. → 이렇게 20~30분만 해도 핵심 지식이 빠르게 쌓입니다.

### 방법 C. 좋은 챗봇 답변을 지식으로 승격
`🤖 AI 도우미`에서 대화하다가 **좋은 답변**이 나오면, 그 답변 아래
**[🧠 지식으로 저장]** 버튼을 누르세요. AI가 질문+답변을 **정리해서** 지식으로 쌓습니다.

---

## 5. 쌓은 지식이 실제로 어떻게 쓰이나요?

`🤖 AI 도우미`에 질문하면 자동으로:

```
질문 → ① 가르친 지식 중 의미가 가까운 것 검색 (RAG)
      + ② 과거 출하이슈 사례 검색 (기존 기능)
      → ③ 둘을 근거로 GPT가 답변  (전문가 지식을 최우선으로 따름)
```

답변 아래에 **`📚 전문가 지식`** 근거 카드가 보이면, 가르친 내용이 실제로 쓰인 것입니다.

---

## 6. 학습(지식 쌓기) 잘하는 요령

- ✅ **사실·기준·절차** 위주로. ("~는 ~이다", "~하면 ~를 먼저 본다")
- ✅ 애매하면 전문가에게 **한 번 더 확인**하고 적기 (잘못된 지식 = 잘못된 답)
- ✅ 기준이 바뀌면 **📚 쌓인 지식 보기**에서 옛 지식을 지우고 새로 추가
- ❌ 잡담·추측·불확실한 내용은 넣지 않기 (AI가 그대로 믿습니다)

> "쓰레기가 들어가면 쓰레기가 나옵니다." — 지식의 **품질**이 답변의 품질을 정합니다.

---

## 7. 자주 묻는 질문

**Q. 돈이 드나요?**
A. OpenAI 사용료가 아주 조금 듭니다. 지식 가르치기(임베딩)는 매우 저렴하고
(1만 글자에 약 몇 원 수준), 답변 생성이 대부분의 비용입니다. 일반 사용엔 부담 없는 수준입니다.

**Q. 인터넷이 안 되면요?**
A. 지식 **저장**은 됩니다(색인만 잠시 미룸). 나중에 인터넷 되면 화면의
**[미색인 N건 색인하기]** 버튼으로 한 번에 검색 가능하게 만들 수 있습니다.
답변 생성과 의미검색은 인터넷(OpenAI)이 필요합니다.

**Q. 키(API Key)는 어디 있나요?**
A. `app/openai_key.txt` 에 들어 있고, 깃허브에는 **절대 올라가지 않습니다**(.gitignore).
X서버 배포로만 각 PC에 전달됩니다.

**Q. 가르친 지식은 어디 저장되나요?**
A. 내 PC의 `data/quality.db` 안 `knowledge` 표. **고객사 실데이터와 같은 곳**이라
깃허브에 안 올라갑니다. 백업은 프로그램의 백업 기능을 쓰세요.

**Q. 다른 팀원 PC와 지식을 공유하려면?**
A. 현재는 PC별로 각자 쌓입니다. 공유가 필요하면 알려주세요(서버 공유 방식 추가 가능).

---

## 8. 개발자 메모 (관리팀 전달용)

- 핵심 모듈: `app/rag.py` (임베딩·코사인 검색·학습·인터뷰·Q&A정리)
- 저장: `knowledge` 테이블 (`app/schema.sql`) — 벡터는 정규화된 JSON 배열, 코사인=내적
- 통합: `app/chatbot.py` `answer()` 가 `rag.search()` 결과를 프롬프트 최우선 근거로 주입
- API: `POST /api/rag/{teach, interview, teach-qa, delete, reindex}`, `GET /api/rag/{stats, list}`
- 화면: `web/js/app.js` `renderRag()` + 네비 `🧠 AI 학습`
- 의존성 **추가 없음**(표준 라이브러리 + 기존 openpyxl 그대로). 임베딩 모델 `text-embedding-3-small`(512차원).
- 규모가 커지면(수만 건+) 전용 벡터DB(sqlite-vec/pgvector 등)로 이관 고려.
